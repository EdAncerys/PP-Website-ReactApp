import React from 'react';

export default function PlayButton({ props }) {
  return (
    <div style={styles.container}>
      <div>Play</div>
    </div>
  );
}

const styles = {
  container: {},
};
