import React from 'react';

export default function PageOne({ props }) {
  return (
    <div style={styles.container}>
      <div>page one</div>
    </div>
  );
}

const styles = {
  container: {},
};
